# G5B BRIEF — Grok to Grok (and CGPT check)

**Job:** G5B. Put 2Z / Drift (Velocity) / Orca on the main V4 dashboard  
**Owner:** Grok  
**Review:** CGPT reads this brief + the evidence pack + the diff. CGPT does not hunt, brief, or write wallet code. Cursor stays out. Grok does not mark this done.  
**Live file:** `crypto-app-v4` only. GitHub is the bible.  
**Not:** Job 1, C1–C6, Job 17, Job 19 ORCA onboard, HOM, V3 writes, new wallets, invented facts.

Oliver rejected the standalone G5 skinny pages on 26 Aug 2026. His words: they MUST be pages on the MAIN V4 dashboard that use the design rules of the whole page. Heavily reduced content is fine. They have to be part of the app, not a separate look.

G5 facts stay. This job is chrome + entry, not new research.

---

## For Oliver (dummy)

Same three guts pages. Now they live inside the main dashboard.

Click 2Z, DRIFT, or ORCA on the hold grid. Same header, same cards, same type, same colours. Not a separate mini-site.

Direct links: `index-v4.html#2z` · `#drift` · `#orca`.

---

## Intention

### G5B is
Fold the CGPT-approved G5 content into `index-v4.html` as reduced `asset-v3-report` views. Same hold-card click path as BTC / FART / SPX. Same alt-hero / alt-group chrome already on the page. Hash routes so Oliver can review live.

### G5B is not
- A new look (no `skinny.css` as the review surface)
- Fat weekly reports / signal pallets / thesis / SELL-WORRIED
- New facts, unlocks, volumes, wallets, Foundation/Jump/Team names
- Job 19 ORCA onboard
- Restyling the dashboard, header, or other cards
- Touching Job 1 `metrics/` or `tests/job1/`

---

## Facts (already approved in G5 — do not change)

**2Z.** Official next unlock DATE / SIZE / WHO = UNKNOWN. Unofficial 2 Oct calendars excluded as the number. Q2 earning line kept. Dest-watch: TwoZ1–TwoZ20 + Wintermute.

**Drift / Velocity.** Same team. Rebrand 1 Jul 2026. Private beta (press 25 Aug 2026). 4 live perps. 24h volume unlisted. Do not say users came back. Dest-watch: Foundation, Team, Team.

**Orca.** DefiLlama volume/fees dated 26 Aug 2026. DEX still takes fees. Two official 12% splits stay separate. Dest-watch: Community treasury, Fee treasury, xORCA vault.

---

## Exact files I will touch

Allowed:

- `index-v4.html` — Oliver asked for this
- `skinny/2z.html` `skinny/drift.html` `skinny/orca.html` — redirect into the main app
- `skinny/skinny.css` — stop shipping as the review surface
- `GROK-G5B-BRIEF.md` (this file)
- `GROK-G5B-EVIDENCE.md`
- `GROK-JOBS.md` — G5B line only

Forbidden:

- `metrics/`, `tests/job1/`, any Job 1 WIP
- `config/wallet.json`, `config/capital.json`, siren start-line, dest-watch JSON writes
- V3, HOM, reports dumps
- Invented dests / invented unlocks / invented Velocity volume

---

## What “done” looks like (CGPT checks)

A. Clicking 2Z / DRIFT / ORCA on the main hold grid opens the reduced page under the same header/chrome.

B. Live URLs are main-app hashes, HTTP 200, not the old skinny standalone look.

C. G5 facts unchanged. UNKNOWN / unlisted / two Orca splits / no users-returned stay.

D. Other hold cards, header, grid look/layout/size/type/colours/spacing unchanged.

E. `metrics/` and `tests/job1/` not in the commit.

F. Grok does not PASS.

---

## What I will not “helpfully” do

- Do not invent a 2Z unlock size
- Do not invent Velocity 24h volume
- Do not say users came back
- Do not start Job 19
- Do not restyle the dashboard
- Do not commit Job 1 WIP
- Do not launch a cloud agent
